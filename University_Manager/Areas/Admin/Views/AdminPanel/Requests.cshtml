@model IEnumerable<University_Manager.Models.RequestViewModel>

@{
    ViewBag.Title = "Requests";
}

<h2>Requests</h2>

<p>
   
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.ActionLink("Rejekt", "Edit", new { id=item.Id, @class=" nav-link text-white" }) |
            @Html.ActionLink("Approve", "Details", new { id=item.Id, @class="nav-brand text-white" }) |

        </td>
    </tr>
}

</table>
